import { JoinTasksPipe } from './join-tasks.pipe';

describe('JoinTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
